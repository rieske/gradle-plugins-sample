plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        projectInfo {
            id = 'com.example.another-buildsrc-plugin'
            implementationClass = 'com.example.AnotherBuildSrcPlugin'
        }
    }
}
